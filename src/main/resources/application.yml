spring:
  application:
    name: twitter
  datasource:
    url: jdbc:mysql://localhost:3307/twitterdb
    username: dev
    password: dev123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
logging:
  level:
    org.hibernate.orm.jdbc.bind: trace